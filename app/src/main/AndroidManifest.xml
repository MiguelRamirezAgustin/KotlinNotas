<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.congnitusfirma">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <permission
        android:name="com.cognitus.signatureex.WRITE_EXTERNAL_STORAGE"
        android:protectionLevel="signature" />

    <application
        android:name=".activities.MisNotasApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Registro"></activity>
        <activity android:name=".activities.FirmaActivity" />
        <activity
            android:name=".activities.ReportesActivity"
            package="com.geeksforgeeks.myfirstkotlinapp" />
        <activity android:name=".activities.MenuActivity" />
        <activity android:name=".activities.RegistroActivity" />
        <activity android:name=".activities.RecuperarContraActivity" />
        <activity android:name=".activities.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
    </application>

</manifest>